#include <stdio.h>

int main()
{

    char date[] = "June 14", *newDate = date;
    const char *a = "Awesome";

    // printf("%s", newDate);
    char x[10];
    printf("Please, enter in your string: ");
    // scanf("%s", x);
    char text[30];
    gets(text);
    puts(text);

    

    return 0;
}

int read_line(char *str, int n)
{
    // the hard part
    int c, i = 0;
    
    while ((c = getchar()) != '\0')
    {
        
    }

}